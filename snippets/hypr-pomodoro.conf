# Pomodoro (uair) keybindings.
# Sourced from hyprland.conf via:  source = ~/.config/hypr/pomodoro.conf

bindd = SUPER ALT, P, Pomodoro start/stop, exec, $HOME/.local/bin/uair-toggle
bindd = SUPER ALT, N, Pomodoro next session, exec, $HOME/.local/bin/uair-jump next
bindd = SUPER ALT, B, Pomodoro previous session, exec, $HOME/.local/bin/uair-jump prev
bindd = SUPER ALT, R, Pomodoro restart cycle, exec, $HOME/.local/bin/uair-jump restart
